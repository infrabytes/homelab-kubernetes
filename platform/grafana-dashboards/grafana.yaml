apiVersion: grafana.integreatly.org/v1beta1
kind: Grafana
metadata:
  name: grafana-cloud
  namespace: grafana-cloud
  labels:
    grafanaInstance: main
spec:
  external:
    url: https://dashingblackberry3531.grafana.net/
    apiKey:
      name: grafana-operator-token
      key: token
