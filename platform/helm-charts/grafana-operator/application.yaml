# ServerSideApply: the Grafana CRD (~600KB) exceeds the client-side-apply annotation limit.
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: grafana-operator-app
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://grafana.github.io/helm-charts
    chart: grafana-operator
    targetRevision: 5.25.0
    helm:
      releaseName: grafana-operator
      values: |
        # New component (2026-09-12): initial estimate; re-measure after two weeks.
        resources:
          requests:
            cpu: 10m
            memory: 128Mi
          limits:
            memory: 256Mi
  destination:
    namespace: grafana-cloud
    server: https://kubernetes.default.svc
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - ServerSideApply=true
