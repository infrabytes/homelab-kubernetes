# Tailscale Operator OAuth client from OpenBao (secret/tailscale-operator); ESO owns the Secret (creationPolicy Owner).
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: operator-oauth
  namespace: tailscale
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: openbao
    kind: ClusterSecretStore
  target:
    name: operator-oauth
    creationPolicy: Owner
  data:
    - secretKey: client_id
      remoteRef:
        key: tailscale-operator
        property: client_id
    - secretKey: client_secret
      remoteRef:
        key: tailscale-operator
        property: client_secret
