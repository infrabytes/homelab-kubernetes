# Terraform state and caches
.terraform/
.terragrunt-cache/
*.tfstate
*.tfstate.*
.terraform.lock.hcl

# Secrets / local overrides
terraform.tfvars
terraform.tfvars.json
*.auto.tfvars
*.auto.tfvars.json
.sops-age-key*
age-key.txt

# Python
__pycache__/
.ruff_cache/

# Locally generated artifacts
artifacts/
# Worktree symlink to the main checkout's artifacts dir (terragrunt needs the kubeconfig)
infra/cluster/artifacts
.worktrees/
