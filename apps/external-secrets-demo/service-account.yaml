# Identity for the namespaced store: ESO webhook requires the store's
# serviceAccountRef to live in the store's namespace; OpenBao role `eso-demo` binds to it.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: eso-demo
  namespace: external-secrets-demo
